services:
  - type: web
    name: mtgit
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
      - key: PORT
        value: "3000"
