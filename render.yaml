services:
  - type: web
    name: mtgit
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        scope: build
        sync: false
      - key: PORT
        value: "3000"
